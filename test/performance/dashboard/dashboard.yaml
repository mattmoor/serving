apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: dashboard
  namespace: default
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/minScale: "1"
    spec:
      containers:
      - image: knative.dev/serving/test/performance/dashboard
